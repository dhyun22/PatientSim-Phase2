# Phase 2 submission — Python 3.10+
# Single `pip install -r requirements.txt` runs the full 18-cell grid +
# disclosure-quality evaluator on the Gemini backend (CPU only, no GPU needed).

# Config & orchestration
hydra-core>=1.3
omegaconf>=2.3
python-dotenv>=1.0
pyyaml>=6.0

# LLM APIs (Gemini is the only one exercised by the Phase 2 grid; openai is
# kept so the Azure / direct-OpenAI fallback paths import cleanly).
google-genai>=1.0
google-auth>=2.30
openai>=1.40

# I/O & utilities
numpy>=1.24
jsonlines>=4.0
tqdm>=4.66
